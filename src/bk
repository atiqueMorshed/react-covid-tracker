* {
  margin: 0;
}

body {
  background-color: #f5f6fa;
}

.app {
  display: grid;
  grid-template-areas:
    "appheader appHeader liveCaseByCountry"
    "infoBoxCollection infoBoxCollection liveCaseByCountry"
    "infoBoxCollection infoBoxCollection liveCaseByCountry"
    "map map liveCaseByCountry"
    "map map worldWideNewCases"
    "map map worldWideNewCases"
    "map map worldWideNewCases";

  .app__appHeader {
    grid-area: appheader;
  }

  .app__infoBoxCollection {
    grid-area: infoBoxCollection;
  }
  .app__map {
    grid-area: map;
  }

  .app__liveCasesByCountry {
    grid-area: liveCaseByCountry;
  }
  .app__worldWideNewCases {
    grid-area: worldWideNewCases;
  }
}
